// Пример: плавная прокрутка (если на странице есть якоря)
(function () {
  const links = document.querySelectorAll('.nav-link');
  links.forEach((a) => {
    a.addEventListener('click', (e) => {
      // Чтобы переход был между страницами, просто позволяем браузеру перейти
      // Этот файл оставлен пустым для простоты
    });
  });
})();